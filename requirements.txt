# Base runtime dependencies
numpy
opencv-python
Pillow
pyyaml
websocket-server
obsws-python
ultralytics
aioconsole
imageio-ffmpeg

# Windows camera support
pygrabber; platform_system == "Windows"

# Optional, not installed by default:
# coremltools  # Required only for .mlpackage / CoreML workflows
